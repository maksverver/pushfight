Metadata about the results that is small enough to commit to the Git repository.

chunk-r0.sha256sum: SHA256 checksums of individual r0 chunks
r0.sha256sum: SHA256 checksum of the concatenated r0 data
